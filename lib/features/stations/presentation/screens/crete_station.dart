import 'package:flutter/material.dart';

class CreateStation extends StatelessWidget {
  const CreateStation({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text("Crear estacion"),
      ),
    );
  }
}